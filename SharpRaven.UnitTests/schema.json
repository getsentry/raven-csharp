{
    "description": "An exception packet for Sentry",
    "type": "object",
    "required": ["level"],
    "properties": {
        "level": {
            "enum": ["fatal", "error", "warning", "info", "debug"]
        }
    }
}